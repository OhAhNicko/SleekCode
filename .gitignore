# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Environment / secrets
.env
.env.*

# Tauri
src-tauri/target/
src-tauri/target.backup/
src-tauri/WixTools/
# scripts/knowledge-mcp-smoke.mjs scratches here (under the repo, not /tmp, so a
# Windows exe launched from WSL can open it). Removed on exit; ignored in case a
# run is killed.
.knowledge-mcp-smoke-*/

# Project docs & tasks (local only)
docs/
tasks/

# Agent git worktrees (never commit)
.claude/worktrees/

# Backups written by scripts/release.sh --draft-groups. The previous manifest is
# already in git history; a .bak riding along in a release commit is just noise.
*.bak

# Reference assets (source-of-truth copies live in src/fonts/)
reference/
