# Commit groups for the next release. Read by scripts/release.sh.
#
# FORMAT
#   A line with NO leading whitespace is a commit subject.
#   Lines indented by 2+ spaces are paths belonging to the subject above them.
#   Blank lines and # comments are ignored.
#
# RULES
#   - Replace every <describe>. The release REFUSES to run while one remains.
#   - A path may appear in only one group (first wins).
#   - Dirty paths you leave out are fine: they land in a final sweep commit.
#   - Paths that are missing, clean, or gitignored are skipped silently, so a
#     slightly stale manifest still works.
#   - Order here is the order the commits are made in.
#
# The subjects below were written by reading each workstream's diff, NOT guessed
# from path names — they become the changelog verbatim.
#
# ONE KNOWN IMPURITY: src-tauri/src/lib.rs carries BOTH Rust workstreams — the
# new `ssh_unlock_keychain` command backing the keychain dialog, and the Codex
# session-lookup fixes. A path can live in only one group, so it sits with the
# Codex fix. Both subjects still reach the changelog, which is what the release
# notes are built from.

feat(keychain): unlock a remote Mac's login keychain by hand after cancelling the automatic prompt
  src/components/UnlockKeychainModal.tsx
  src/lib/unlock-keychain-modal.ts
  src/lib/keychain.ts
  src/components/ServersPanel.tsx
  src/lib/menu/providers/rows.ts
  src/lib/menu-icons.tsx
  src/index.css
  src/App.tsx

feat(terminal): draw the TUI scrollbar for Codex and Gemini panes, not just Claude
  src/native-term/TuiScrollbar.tsx
  src/native-term/tui-scroll-model.ts

feat(terminal): accelerate a fast wheel flick inside mouse-tracking TUIs, so a fullscreen CLI no longer crawls
  src-tauri/src/native_term/window/win32.rs
  src/components/TerminalPaneXterm.tsx

fix(context): take the context percentage from the CLI's own statusline, so a pane can no longer report a confident wrong 100%
  src/lib/cli-statusline.ts
  src/hooks/useSessionContext.ts
  src/components/TerminalPaneNative.tsx

fix(codex): match session cwd case-insensitively and stop reporting older rollouts as deleted
  src-tauri/src/lib.rs
  scripts/made-codex-diag.sh
  scripts/made-codex-diag2.sh
