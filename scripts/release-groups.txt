# Commit groups for the next release. Read by scripts/release.sh.
#
# FORMAT
#   A line with NO leading whitespace is a commit subject.
#   Lines indented by 2+ spaces are paths belonging to the subject above them.
#   Blank lines and # comments are ignored.
#
# RULES
#   - Replace every <describe>. The release REFUSES to run while one remains.
#   - A path may appear in only one group (first wins).
#   - Dirty paths you leave out are fine: they land in a final sweep commit.
#   - Paths that are missing, clean, or gitignored are skipped silently, so a
#     slightly stale manifest still works.
#   - Order here is the order the commits are made in.
#
# Groups below were GUESSED from file paths. Rewrite the subjects to say what
# the work actually did — they become the release changelog verbatim.

fix(ssh): remote Claude panes ask for the keychain password again — the unlock preamble read a bare ~/.claude/.credentials.json as proof Claude was signed in, but an MCP server's OAuth writes that same file, so on a Mac whose Claude login lives in the Keychain every pane skipped the prompt silently and came up at the login screen
  src/lib/keychain.ts

fix(window): the light "border skeleton" around the production window is gone — our own NCCALCSIZE per-edge clamp inset the client rect whenever the window sat a frame-width past the work area, and with NC painting suppressed that strip was never painted at all; restored windows now claim the full window rect, maximized still hard-sets the work area
  src-tauri/src/lib.rs

feat(terminal): pane-header buttons wait out a 200ms hover before sliding in, so a pointer crossing the header no longer re-flows it, and a new "Slide-in header buttons" setting keeps them permanently boxed to fade in place instead
  src/index.css
  src/components/TerminalHeader.tsx
  src/components/SettingsPane.tsx
  src/store/themeSlice.ts
  src/store/index.ts
