# Commit groups for the next release. Read by scripts/release.sh.
#
# FORMAT
#   A line with NO leading whitespace is a commit subject.
#   Lines indented by 2+ spaces are paths belonging to the subject above them.
#   Blank lines and # comments are ignored.
#
# RULES
#   - Replace every <describe>. The release REFUSES to run while one remains.
#   - A path may appear in only one group (first wins).
#   - Dirty paths you leave out are fine: they land in a final sweep commit.
#   - Paths that are missing, clean, or gitignored are skipped silently, so a
#     slightly stale manifest still works.
#   - Order here is the order the commits are made in.
#
# The subjects below were written by reading each workstream's diff (mostly the
# comments the author left), NOT guessed from path names. Two of them are
# first-hand; the rest are an outside reading of someone else's WIP, so check
# they match what you actually did before releasing — they become the changelog
# verbatim.
#
# Deliberately left OUT, so they land in the sweep commit: App.tsx, index.css,
# GlobalContextMenu.tsx, src-tauri/src/lib.rs, tauri.conf.json and AGENTS.md.
# Each spans several of the workstreams below and I could not honestly assign
# it to one. Give them their own group if you want them in the changelog.

feat(git): read-only git for projects on an SSH server
  src/lib/git-invoke.ts
  src/components/CodeReviewPane.tsx
  src/components/CodeReviewDiffView.tsx
  src/components/GitStatusBar.tsx
  src/lib/git-branch-cache.ts
  src/lib/menu/providers/terminal.ts

feat(dev-server): run several dev servers per project and choose which one the globe icons open
  src/lib/spawn-dev-server.ts
  src/lib/dev-server-lookup.ts
  src/components/DevServerTab.tsx
  src/components/DevServerTerminalHost.tsx
  src/components/ServersPanel.tsx
  src/components/BrowserPreview.tsx
  src/components/TabBar.tsx
  src/components/VerticalTabBar.tsx
  src/lib/menu/providers/rows.ts
  src/store/recentProjectsSlice.ts

feat(session): cap CLI-supplied session titles and confirm removal in the session picker
  src/lib/session-title.ts
  src/lib/session-exists.ts
  src/lib/sessions-index.ts
  src/lib/context-parser.ts
  src/hooks/useSessionContext.ts
  src/components/TerminalHeader.tsx
  src/overlay/OverlayRoot.tsx

feat(terminal): share one prompt-line detector between both pane backends
  src/lib/prompt-lines.ts
  src/components/TerminalPaneNative.tsx
  src/components/TerminalPaneXterm.tsx
  src/native-term/TuiScrollbar.tsx
  src/native-term/tui-scroll-model.ts
  src/lib/native-term-bridge.ts
  src-tauri/src/native_term/events.rs
  src-tauri/src/native_term/mod.rs
  src-tauri/src/native_term/window/mod.rs
  src-tauri/src/native_term/window/win32.rs

feat(theme): give diff counts semantic colors that survive a red-accent theme
  src/lib/themes.ts

fix(modals): center dialogs on an optical 38% line instead of a fixed top offset
  src/lib/modal-layout.ts
  src/components/PromptModal.tsx
  src/components/NewJiraTicketModal.tsx
  src/components/JiraProjectModal.tsx
  src/components/ChangelogModal.tsx
  src/components/ClaudeTokenWizardModal.tsx
  src/components/ClearDataModal.tsx
  src/components/CommandPalette.tsx
  src/components/ConnectToGitHubModal.tsx
  src/components/CreateProjectModal.tsx
  src/components/CreatePullRequestModal.tsx
  src/components/ImagePreviewModal.tsx
  src/components/KeyboardShortcutsModal.tsx
  src/components/ReleaseModal.tsx
  src/components/SshKeySetupWizardModal.tsx
  src/components/TemplatePicker.tsx
  src/components/WelcomeModal.tsx

chore(release): hand the release skill off to scripts/release.sh outside WSL
  .claude/skills/release/SKILL.md
  scripts/release-groups.txt
  .gitignore
