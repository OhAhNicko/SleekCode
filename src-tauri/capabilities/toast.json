{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "toast",
  "description": "Capability for the custom notification popup window: cross-window events (card payloads in, click actions out). Custom app commands (toast_window_place) are allowed by default and need no entry here.",
  "windows": ["toast"],
  "permissions": ["core:event:default"]
}
